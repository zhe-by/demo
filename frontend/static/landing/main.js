(function () {
    // read email input
    // debounce input
    // ajax user existing
    // if exist show login
    // else show register
    // listen password retype
    // check equality

    // outputs:
    // login or register
    //

    // empty(isLoading)
    // login(errors)
    // register(errors)
}());
